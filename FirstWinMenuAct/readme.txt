这个例子展示如何利用windows 的API创建一个带菜单的窗体。
当点击菜单项时，弹出一个消息窗体。

菜单是程序的运行时资源，程序所用的菜单，图标，动态加载的字符串都可以是程序的资源。

资源文件是rc文件，此例子是myresource.rc，rc文件需要编译为二进制，然后在链接（linker）时将编译后的程序的二进制文件和资源文件的二进制文件进行链接。

resource.h 为便于使用，定义了资源文件中使用的常量。

gcc环境下，使用windres对资源文件进行编译。

windows有自带的rc编译器，与其自身的编译器配合使用。
